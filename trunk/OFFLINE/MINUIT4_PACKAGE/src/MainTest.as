/*** Written by :* @author Floz* www.floz.fr || www.minuit4.fr*/package {	import fr.minuit4.core.datas.dynamics.DynamicXML;	import fr.minuit4.net.loaders.types.DataLoader;	import flash.display.Sprite;	import flash.events.Event;	public class MainTest extends Sprite	{			// - PRIVATE VARIABLES -----------------------------------------------------------				// - PUBLIC VARIABLES ------------------------------------------------------------				// - CONSTRUCTOR -----------------------------------------------------------------				public function MainTest()		{			var loader:DataLoader = new DataLoader();			loader.addEventListener( Event.COMPLETE, onLoadComplete );			loader.load( "xml/conf.xml" );		}				private function onLoadComplete(event:Event):void		{			trace( "toto" );			var loader:DataLoader = event.currentTarget as DataLoader;						var d:DynamicXML = new DynamicXML( XML( loader.getItemLoaded() ) );			trace( d.getData( "testnode" ) );		}				// - EVENTS HANDLERS -------------------------------------------------------------				// - PRIVATE METHODS -------------------------------------------------------------				// - PUBLIC METHODS --------------------------------------------------------------				// - GETTERS & SETTERS -----------------------------------------------------------		}	}