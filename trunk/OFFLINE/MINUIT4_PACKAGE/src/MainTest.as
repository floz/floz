/*** Written by :* @author Floz* www.floz.fr || www.minuit4.fr*/package {	import fr.minuit4.core.conf.Configuration;	import flash.display.Bitmap;	import flash.display.Sprite;	import flash.events.Event;	public class MainTest extends Sprite	{			// - PRIVATE VARIABLES -----------------------------------------------------------				private var _conf:Configuration;				// - PUBLIC VARIABLES ------------------------------------------------------------				// - CONSTRUCTOR -----------------------------------------------------------------				public function MainTest()		{			_conf = Configuration.getInstance();			_conf.addEventListener( Event.COMPLETE, onLoadComplete );			_conf.load( "xml/conf.xml" );		}		private function onLoadComplete(event:Event):void		{			trace( _conf.getProperty( "testnode" ) );			_conf.setProperty( "minuit4", "00h04" );			trace( _conf.getProperty( "testnode" ) );			trace( _conf.getProperty( "chapin" ) );						trace( Configuration.pathIMG );						var x:XML = XML( _conf.getProperty( "datas1" ) );			trace( x );									var b:Bitmap = _conf.getProperty( "chapin" );			addChild( b );		}				// - EVENTS HANDLERS -------------------------------------------------------------				// - PRIVATE METHODS -------------------------------------------------------------				// - PUBLIC METHODS --------------------------------------------------------------				// - GETTERS & SETTERS -----------------------------------------------------------		}	}