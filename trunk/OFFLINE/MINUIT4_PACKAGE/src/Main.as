/*** Written by :* @author Floz* www.floz.fr || www.minuit4.fr*/package {	import flash.events.ProgressEvent;	import fr.minuit4.core.conf.Configuration;	import flash.display.Bitmap;	import flash.display.Sprite;	import flash.events.Event;	public class Main extends Sprite	{			// - PRIVATE VARIABLES -----------------------------------------------------------				// - PUBLIC VARIABLES ------------------------------------------------------------				// - CONSTRUCTOR -----------------------------------------------------------------				public function Main()		{			var conf:Configuration = Configuration.getInstance();			conf.addEventListener( ProgressEvent.PROGRESS, onProgress );			conf.addEventListener( Event.COMPLETE, onLoadComplete );			conf.loadXML( "xml/conf.xml" );		}		// - EVENTS HANDLERS -------------------------------------------------------------				private function onProgress( e:ProgressEvent ):void		{			trace( "percent : " + e.bytesLoaded / e.bytesTotal );		}				private function onLoadComplete( e:Event ):void		{			var conf:Configuration = e.currentTarget as Configuration;			trace( conf.getString( "xmlPath" ) );			conf.setProperty( "imgPath",  "${testnode}/" + conf.getString( "imgPath" ) );			trace( conf.getString( "xmlPath" ) );						trace( conf.getXML( "testXml" ).items.item[ 0 ] );						trace( "DATAS 1 : " + conf.getXML( "datas1" ) );			trace( conf.getMedia( "chapin" ) );			addChild( Bitmap( conf.getMedia( "chapin" ) ) );		}				// - PRIVATE METHODS -------------------------------------------------------------				// - PUBLIC METHODS --------------------------------------------------------------				// - GETTERS & SETTERS -----------------------------------------------------------		}	}