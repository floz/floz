/*** Written by :* @author Floz* www.floz.fr || www.minuit4.fr*/package {	import aze.motion.Eaze;	import fr.minuit4.motion.M4Tween;	import gs.TweenLite;	import flash.display.Graphics;	import flash.display.Sprite;	import flash.events.MouseEvent;	import flash.utils.getTimer;	public class MainM4Tween2 extends Sprite	{			// - PRIVATE VARIABLES -----------------------------------------------------------				private var _m4:Sprite = getSquare( 0x444444 );		private var _tweenlite:Sprite = getSquare( 0x00ff00 );		private var _eaze:Sprite = getSquare( 0x0000ff );		private var _debut:Number;				// - PUBLIC VARIABLES ------------------------------------------------------------				// - CONSTRUCTOR -----------------------------------------------------------------				public function MainM4Tween2()		{						stage.addEventListener( MouseEvent.CLICK, onClick );		}				public function getSquare( color:uint ):Sprite		{			var s:Sprite = new Sprite();			var g:Graphics = s.graphics;			g.beginFill( color );			g.drawRect( 0, 0, 30, 30 );			g.endFill();			return addChild( s ) as Sprite;		}				// - EVENTS HANDLERS -------------------------------------------------------------				private function onClick( e:MouseEvent ):void		{			_debut = getTimer();			M4Tween.to( _m4, .5, { x: e.stageX, y: e.stageY } ).onComplete( function():void { trace( getTimer() - _debut ); } );			Eaze.to( _eaze, .5, { x: e.stageX + 40, y: e.stageY } ).onComplete( function():void { trace( getTimer() - _debut ); } );			TweenLite.to( _tweenlite, .5, { x: e.stageX, y: e.stageY + 40, onComplete: function():void { trace( getTimer() - _debut ); } } );		}				// - PRIVATE METHODS -------------------------------------------------------------				// - PUBLIC METHODS --------------------------------------------------------------				// - GETTERS & SETTERS -----------------------------------------------------------		}	}